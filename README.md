# DataStructure — C 语言数据结构工程实现

基于严蔚敏《数据结构（C语言版）》/王道教材体系，按 C 语言工程规范实现的
65 个数据结构模块。每个模块完全独立、自包含，可单独编译运行。

## 🖥️ 教程站点（本地预览）

本项目配有 VitePress 教程站：65 个模块的讲解、源码展示与排序算法动画。

```bash
cd docs && npm install
npm run docs:dev      # http://localhost:5173
```

其他命令：`cd docs && npm run docs:build`（构建静态产物）、`cd docs && npm run test`（动画步骤单测）、
`cd docs && npm run check`（讲解页完整性检查）、`cd docs && npm run gen:pages`（为新模块生成骨架页）。

## 目录导航

| 分类 | 模块 | 进入目录后编译 | 常用于（现实用途） |
|---|---|---|---|
| 01_线性表 | 静态顺序表 / 动态顺序表 / 单链表 / 双链表 / 循环链表(含约瑟夫环) / 静态链表 | `gcc -Wall -Wextra -std=c99 *.c -o demo.exe` | 一切"顺序/列表"数据：播放列表、浏览历史、通讯录 |
| 02_栈 | 顺序栈 / 两栈共享空间 / 链栈 / 栈的应用：括号匹配、表达式求值、递归 | 同上 | 后进先出：函数调用栈、撤销/重做、浏览器后退、括号与表达式求值 |
| 03_队列 | 循环顺序队列 / 链队列 / 队列的应用：舞伴问题 | 同上 | 先进先出：打印/任务排队、消息队列、银行叫号 |
| 04_特殊矩阵压缩存储 | 对称矩阵 / 三角矩阵 / 对角矩阵 / 稀疏矩阵-三元组 / 稀疏矩阵-十字链表 | 同上 | 省内存地存稀疏/规则数据：图像、图的邻接关系、稀疏矩阵运算 |
| 05_串 | 定长顺序串 / 堆分配串 / 块链串 / 朴素模式匹配(BF) / KMP(next+nextval) | 同上 | 文本处理：搜索匹配(KMP)、查找替换、DNA 序列比对、日志关键字检索 |
| 06_树与二叉树 | 树的存储结构(双亲/孩子/孩子兄弟) / 二叉树顺序存储 / 二叉树链式实现(递归+非递归遍历) / 线索二叉树 / 森林与二叉树转换 / 哈夫曼树(编码) / 并查集 | 同上 | 层级/关系数据：文件系统目录、DOM 树、编译器语法树、哈夫曼压缩、数据库 B 树索引、并查集(连通性) |
| 07_图 | 邻接矩阵(DFS/BFS) / 邻接表(非递归DFS/BFS) / 十字链表 / 邻接多重表 / 最小生成树(Prim/Kruskal) / 最短路径(Dijkstra/Floyd) / 拓扑排序(Kahn/DFS逆后序) / 关键路径(AOE) | 同上 | 网络/关系建模：地图导航(最短路)、社交网络、物流(最小生成树/关键路径)、依赖调度(拓扑) |
| 08_查找 | 顺序查找 / 折半查找(插值、斐波那契) / 分块查找 / 二叉排序树 / 平衡二叉树(AVL) / 红黑树 / B树 / B+树 / 散列函数 / 散列表-拉链法 / 散列表-开放定址法 / 散列查找性能分析 | 同上 | 从海量数据快速定位：数据库/字典索引、登录校验、缓存、倒排索引、哈希表 |
| 09_排序 | 插入排序 / 希尔排序 / 冒泡排序 / 快速排序 / 简单选择排序 / 堆排序 / 归并排序 / 计数排序 / 基数排序 / 外部排序(败者树/置换选择/最佳归并树/外部多路归并) | 同上 | 让数据有序以支撑查找与展示：排行榜、按价/时间排序、数据库 ORDER BY、海量数据外部排序 |

在任一模块文件夹内执行上述 gcc 命令，然后运行 `./demo.exe` 即可看到
断言测试与演示输出（每个 main.c 末尾打印"全部测试通过"）。

测试分两层：每个模块的 `main.c` 内置断言测试与运行演示（末尾打印"全部测试通过"）；
若干**算法类**模块还在 `main.c` 内做了随机对拍/压力测试——BF vs KMP 四方对拍、
递归 vs 非递归遍历对拍、Dijkstra/Floyd/Bellman-Ford 三方对拍、Prim vs Kruskal 权值互拍、
MST 与全组合枚举最优解对拍、哈夫曼 WPL 与合并果子对拍、拓扑序合法性验证、
AOE 工期与最长路径 DP 对拍等。全模块测试汇总见 `TEST_REPORT.md`。

## 建议学习顺序（零基础·约一个月）

没基础也完全能学：本库每个模块都独立自包含、可单独编译运行，**逐个啃即可**。
建议按难度从低到高推进，先吃透"范式"（不透明指针、错误处理、destroy 语义、注释里的"为什么"），
再逐个刷算法。分类里挑代表模块读透即可，不必逐行读完全部 65 个模块。

| 阶段 | 内容 | 重心 |
|---|---|---|
| 第1周 | C 基础 + 01 线性表 + 02 栈 / 03 队列 | 读得懂 .h/.c 分离与状态码，会写插入/删除/查找 |
| 第2周 | 04 矩阵 + 05 串(KMP) + 06 树 | 递归与非递归遍历、线索化、哈夫曼 |
| 第3周 | 07 图 | DFS/BFS、最小生成树、最短路、拓扑/关键路径 |
| 第4周 | 08 查找 + 09 排序 | BST/AVL/红黑/B 树、内外排序与对拍 |

> 每天能挤多少算多少，关键是**每个模块亲自动手复现一遍核心函数**，效果远好于只看。
> 并行忙其他事也没关系：模块互不依赖，随时暂停随时接上；看不懂的先跳过，回头再补。

## 每个模块的文件结构

```
模块文件夹/
├── xxx.h      # 接口：头文件守卫、类型声明、API 文档注释
├── xxx.c      # 实现：结构体定义（不透明指针）、算法逻辑
└── main.c     # 断言测试 + 运行演示
```

## 贯穿全部模块的工程规范（学习重点）

1. **头文件守卫**：三段式 `#ifndef / #define / #endif`，防重复包含
2. **接口/实现分离**：`.h` 只放声明，`.c` 放全部实现
3. **不透明指针**：头文件仅 `typedef struct Xxx Xxx;`，结构体定义藏在
   `.c` 中，外部只能通过 API 操作 —— C 语言实现封装的核心手法
   （例外：定长数组且无隐藏不变量的结构——静态顺序表/静态链表/顺序栈/
   共享栈/循环顺序队列——直接在 .h 暴露布局，头文件注释均援引此条款）
4. **统一状态码**：各模块自带一份 `DsStatus` + `DsResult`（`code` + `message`），
   枚举数值从头一致（DS_OK = 0 起），成员按模块所需裁剪——查找类含
   DS_NOT_FOUND、树/图类只保留用得上的几值，不用裸返回值
5. **destroy 语义约定**：堆对象（init 用 `T**`）销毁用
   `void xxx_destroy(T **obj)`，释放后自动置 NULL；栈对象（init 用 `T*`）
   销毁仅复位字段。调用方从此不必担心悬空指针
6. **const 正确性**：只读参数与打印函数一律 `const` 限定
7. **static 最小暴露**：实现内部辅助函数全部 `static`，不污染全局命名空间
8. **类前缀命名**：同类结构共用前缀（`list_` / `stack_` / `queue_` /
   `matrix_` / `seqlist_`），类型名大驼峰，函数名蛇形小写
9. **malloc 惯用法**：统一 `p = malloc(sizeof *p);` 无强转写法
10. **教学型注释**：文件头讲设计要点与复杂度，API 注释含
   @brief/@param/@return/@note 四要素，实现注释解释"为什么"与易错点

## ADT 覆盖说明

各结构除基础操作外，链表类均提供 `list_clear`（清空≠销毁）与
`list_traverse`（回调式遍历）；双链表提供 O(1) 的
`list_prior_elem`/`list_next_elem`（双链表存在的意义）；稀疏矩阵-三元组
提供 `matrix_add`（行序归并相加，含相加抵消为 0 的经典考点）与
普通/快速双版本转置。

## 命名速查

| 类 | 函数前缀 | 类型名 |
|---|---|---|
| 顺序表（静态/动态） | `seqlist_` | `SeqList` |
| 链表（单/双/循环/静态） | `list_` | `SinglyLinkedList` 等 |
| 栈（顺序/链/共享） | `stack_` | `SeqStack` / `LinkStack` / `SharedStack` |
| 队列（顺序/链） | `queue_` | `SeqQueue` / `LinkQueue` |
| 矩阵 | `matrix_` | `SymmetricMatrix` 等 |
| 串（定长/堆分配/块链） | `ss_` / `hs_` / `bs_` | `StaticString` / `HeapString` / `BlockString` |
| 模式匹配 | `bf_` / `kmp_` | — |
| 树的三种存储 | `pt_` / `clt_` / `cst_` | `ParentTree` / `ChildListTree` / `CSTree` |
| 二叉树（顺序/链式） | `sbt_` / `bitree_` | `SeqBinaryTree` / `BiTree` |
| 线索二叉树 | `tt_` | `ThreadTree` |
| 森林/二叉树互转 | `forest_` / `bt_` | `Forest` / `CSBinaryTree` |
| 哈夫曼树 | `ht_` | `HuffmanTree` |
| 并查集 | `uf_` | `UnionFind` |
| 图（邻接矩阵/邻接表） | `mg_` / `ag_` | `MatrixGraph` / `AdjListGraph` |
| 十字链表 / 邻接多重表 | `og_` / `aml_` | `OrthGraph` / `AmlGraph` |
| 最小生成树 / 最短路径 | `mstg_` `mst_` / `spg_` `sp_` | `MstGraph` / `SpGraph` |
| 拓扑排序 / 关键路径 | `tg_` `topo_` / `aoe_` `cp_` | `TopoGraph` / `AoeGraph` |
| 应用算法 | 按功能 | `bracket_match()`、`infix_to_postfix()` 等 |
